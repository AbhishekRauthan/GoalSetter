import { ErrorExeptionFilter } from './error-exeption.filter';

describe('ErrorExeptionFilter', () => {
  it('should be defined', () => {
    expect(new ErrorExeptionFilter()).toBeDefined();
  });
});
